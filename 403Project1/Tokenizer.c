//#include <regex.h>
#include <stdio.h>

_Bool tokenizer(struct lexics *aLex, int *numLex, FILE *inf) {
     fopen(inf, "r");
     fscanf()
     int pos = 0;
     for (pos < )
     

    //if error return false else return true
}